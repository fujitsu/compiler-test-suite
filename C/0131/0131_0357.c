#include <stdlib.h>
#include <stdio.h>
#include <math.h>

int main()
{
	int j,i=0,n=1,a[10];
	j=-i;
	for(j=i=n;i<8;i++)
	{
		a[i]=i;
	}
	for(i=1;i<8;i++)
	{
		printf("**** a[%d] = %d ****\n",i,a[i]);
	}
	exit (0);
}
