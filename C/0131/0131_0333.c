#include <stdlib.h>
#include <stdio.h>
#include <math.h>

int func();
int main()
{
	int i,x=0;
	for(i=func(x);i<10;i++)
	{
		printf(" *** i = %d *** \n",i);
	}
	exit (0);
}
int func(x)
int x;
{
	return 0;
}
