#include <stdlib.h>
#include <stdio.h>
#include <math.h>
int get(int a);
int main()
{
  int foo;

  foo=get(0);
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 
  ++foo; 

  if (foo != 40)
    {
      printf("NG \n");
    }else{
      printf("OK \n");
    }
  exit(0);
}
int get(a)
int a;
{
 return a;
}





