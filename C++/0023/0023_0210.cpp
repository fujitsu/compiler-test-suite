#include <cassert>

class T
{
    public:
        int mem ;
};

int main()
{
     switch(int c = 0;c)
     {
      case 1:
          {
             if ( T t{10};t.mem > 0)
               {
                  assert(t.mem == 10);
               }
          }
     }
}
