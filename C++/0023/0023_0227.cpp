#include <cassert>

class T
{ 
    public:
       int mem; 
};

int main()
{
   for(int i = 0;i < 2;i++)
   {
    switch(T t{1};t.mem)
    {
      case 1:
       {
        assert(t.mem == 1);
       }
    }
   }
}
