file(COPY lit.local.cfg DESTINATION "${CMAKE_CURRENT_BINARY_DIR}")
llvm_singlesource_fujitsu(PREFIX "Fujitsu-Fortran-")
llvm_set_own_fortran_module_directory()
if(TEST_SUITE_FUJITSU_ENABLE_${llvm_test_lang}_NON_OPENMP)
  set_property(SOURCE 0661_0052.f APPEND PROPERTY COMPILE_OPTIONS "-fxor-operator")
  set_property(SOURCE 0661_0053.f APPEND PROPERTY COMPILE_OPTIONS "-fxor-operator")
endif()
