MODULE M
TYPE TY
    INTEGER :: X=9
     REAL :: Y=8
END TYPE TY
INTEGER::EE
INTERFACE  WRITE(FORMATTED)
SUBROUTINE SUB (DTV, UNIT, IOTYPE, V_LIST, IOSTAT,IOMSG)
    IMPORT TY
    CLASS(TY), INTENT(IN)            :: DTV
    INTEGER, INTENT(IN)            :: UNIT
    CHARACTER(LEN=*), INTENT(IN)    :: IOTYPE
    INTEGER, INTENT(IN)             :: V_LIST (:)
    INTEGER, INTENT(OUT)            :: IOSTAT
    CHARACTER(LEN=*), INTENT(INOUT) :: IOMSG
END SUBROUTINE SUB
END INTERFACE
END MODULE M

PROGRAM MAIN

USE M

INTERFACE
  FUNCTION FUN(D)
  IMPORT TY
  TYPE (TY) :: D,FUN
  END FUNCTION
  END INTERFACE

  TYPE (TY) :: T1
  CALL SS(FUN)

  CONTAINS

  SUBROUTINE SS(DFUN)
 INTEGER::A,B
  INTERFACE
  FUNCTION DFUN(DD)
  IMPORT TY
  TYPE (TY) :: DD,DFUN
  END FUNCTION
 END INTERFACE
  A=8
  B=6
  OPEN(UNIT =70,FILE='fort.39',FORM='FORMATTED')


  WRITE(UNIT=70,*)A,DFUN(T1),B

CLOSE(UNIT=70,status='delete')
        IF(EE==20)PRINT*,'PASS'
  END SUBROUTINE
END PROGRAM

       FUNCTION FUN(D)
        USE M
        TYPE (TY) :: D,FUN
        FUN=D
        FUN%X = FUN%X+10
        FUN%Y = FUN%Y+10
      END FUNCTION




SUBROUTINE SUB (DTV, UNIT, IOTYPE, V_LIST, IOSTAT,IOMSG)
    USE M
    CLASS(TY), INTENT(IN)            :: DTV
    INTEGER, INTENT(IN)            :: UNIT
    CHARACTER(LEN=*), INTENT(IN)    :: IOTYPE
    INTEGER, INTENT(IN)             :: V_LIST (:)
    INTEGER, INTENT(OUT)            :: IOSTAT
    CHARACTER(LEN=*), INTENT(INOUT) :: IOMSG
    EE=DTV%X+1
    WRITE (UNIT=UNIT,*, IOSTAT=IOSTAT, IOMSG=IOMSG) DTV%X+1,IOTYPE,V_LIST
END SUBROUTINE SUB

