IMPLICIT NONE
  REAL SEC
  INTRINSIC SYSTEM_CLOCK ! FORTRAN90 INTRINSIC ROUTINE
  INTEGER(4) CNT, CNT_RATE ! (INT64)
  !--------------
  CALL SYSTEM_CLOCK (COUNT=CNT, COUNT_RATE=CNT_RATE) 
!SEC = DBLE / CNT_RATE

 write(4,*) 'CNT=', CNT, ', CNT_RATE=', CNT_RATE
! write(4,*) 'SEC=', SEC 
print *,'sngg742j : pass'
END
